Instructions for bulk upload
===================================================================

Start from the sample file and only change the values, not the columns.

  * Easiest route: set the six settings on the upload page, then press "Download template with
    these settings". You get a file with those columns already filled in on every row and the
    rest blank, so all you type is the product's own details.
  * The file has 26 product columns (A to Z) followed by a block of 5 variant columns (AA to AE).
    So a file is 31 columns wide for one variant, 36 for two, 41 for three, and so on.
  * Do not add or remove columns. Every column is read by its position, so one extra or missing
    column shifts everything after it. The upload is refused if the count does not fit.
  * Comma, semicolon and tab separated files are all accepted.
  * Leave any optional column blank if it does not apply. Blank is always safe.
  * The whole file is checked before anything is saved. If one row is wrong, nothing is
    imported and the row number is reported.

-------------------------------------------------------------------
Settings you never have to decode
-------------------------------------------------------------------
Columns T to Z used to be number codes. They are written in plain words now, and the template
you download from the upload page arrives with them already filled in from the choices you made
there. You only touch them if you want one product to differ from the rest.

If you leave one of those cells blank, the setting chosen on the upload page is used instead.

Two more are worked out for you and are never asked for:

  * Stock tracking level - filling the product stock column tracks stock for the product;
    filling the variant stock columns tracks it per variant; leaving both blank means stock is
    not tracked at all.
  * Availability - a product is available unless you set its stock to 0.

There is no seller_id column. Products are always created under the account you are logged in
with.

-------------------------------------------------------------------
Product columns
-------------------------------------------------------------------

A.  category_id - mandatory. The Category ID shown on the Categories page.

B.  product_type - mandatory. Exactly simple_product or variable_product.
      simple_product   - one price, no options. Fill one variant block.
      variable_product - has options such as size or colour. Fill one variant block per option
                         and give attribute_value_ids on each.

C.  name - mandatory. The product name.

D.  short_description - optional. One line shown next to the product.

E.  description - optional. The full description.

F.  image - mandatory. The main image. Copy the path from the Media page,
      for example uploads/media/2021/07.jpg

G.  other_images - optional. More image paths, separated with commas.

H.  tags - optional. Search words, separated with commas.

I.  sku - optional. Your own code for the product.

J.  stock - optional. How many you have. Fill this when you keep one stock figure for the whole
      product. Leave it blank and use the variant stock column instead when each option has its
      own stock. Leave both blank if you do not track stock.

K.  tax - optional. The tax ID from Products > Tax.

L.  made_in - optional. For example India.

M.  warranty_period - optional. Free text, for example 6 months.

N.  guarantee_period - optional. Free text, for example 1 year.

O.  video_type - optional. Exactly youtube or vimeo, or blank.

P.  video - optional. The video link.

Q.  minimum_order_quantity - optional. The fewest a customer can buy. Blank means 1.

R.  quantity_step_size - optional. Customers buy in multiples of this. Blank means 1.

S.  total_allowed_quantity - optional. The most one customer may buy. Blank means no limit.

T.  cod_allowed - Yes or No. Whether customers can pay cash on delivery.

U.  prices_include_tax - Yes or No. Whether the prices you typed already include tax.

V.  returnable - Yes or No.

W.  cancellable_until - No, Until received, Until processed, or Until shipped.

X.  food_type - Not a food product, Vegetarian, or Non-vegetarian.

Y.  delivery_area - Everywhere, Only these pincodes, Except these pincodes, or
      Not deliverable yet.

Z.  pincodes - only needed when delivery_area is "Only these pincodes" or "Except these
      pincodes". Separate them with commas or spaces.

-------------------------------------------------------------------
Variant columns - repeat these 5 for every extra variant
-------------------------------------------------------------------

AA. attribute_value_ids - required for variable_product, leave blank for simple_product. The
      IDs from Products > Attribute Values, for example 3,4

AB. price - mandatory. Numbers only, no currency symbol. A variant counts as filled in when
      this column has a value, so this is the column that decides how many variants a row has.

AC. special_price - optional. The discounted price. It must be lower than the price.

AD. sku_variant - optional. Your own code for this variant.

AE. stock_variant - optional. Stock for this variant. Use this instead of the product stock
      column when each variant has its own stock.
